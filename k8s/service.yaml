apiVersion: v1
kind: Service
metadata:
  name: ai-agent-security-eval
  labels:
    app: ai-agent-security-eval
spec:
  type: ClusterIP
  selector:
    app: ai-agent-security-eval
  ports:
    - name: http
      port: 8000
      targetPort: http

